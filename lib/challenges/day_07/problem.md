# Day 07 – Count Vowels in a String

## Problem
Write a Dart function that takes a string as input and returns the total number of vowels (`a`, `e`, `i`, `o`, `u`) in it.  
The function should be **case-insensitive**.

## Example
Input: "Flutter is Awesome"  
Output: 6
