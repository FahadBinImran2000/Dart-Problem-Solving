## Explanation

1. Take a list of integers as input.
2. Create a new list to store the result.
3. Loop through the input list and add all non-zero elements to the result list.
4. Loop through the input list again and add all 0 values to the end of the result list.
5. Return the final list where all non-zero numbers keep their original order and all zeros are moved to the end.