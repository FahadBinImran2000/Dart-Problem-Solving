# Explanation

1. Take an integer number as input.
2. If the number is less than 0, return 0 because factorial is not defined for negative numbers.
3. Create a variable factorial and initialize it with 1.
4. Loop from 1 to the given number.
5. Multiply the current value of factorial with the loop variable in each iteration.
6. After the loop ends, return the final factorial value.