## Explanation

1. Take a list of integers as input.
2. Create a new list to store unique numbers.
3. Iterate through each number in the input list.
4. If the new list does not already contain the current number, add it, otherwise skip it.
5. Return the list containing only unique numbers.
