# Explanation

1. Take a string as input.
2. Create a variable to store the lowercase converted string.
3. Create a variable to store the reversed string.
4. Loop through the string from the last character to the first and build the reversed string.
5. Compare the original lowercase string with the reversed string.
6. If both are equal then return true, otherwise return false.