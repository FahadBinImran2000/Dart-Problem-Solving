# Day 10 – Check if a String is a Palindrome

## Problem
Write a Dart function that takes a string and returns `true` if it is a palindrome, otherwise return `false`.

A palindrome reads the same forward and backward.

The check should be **case-insensitive**.

## Example
Input: "level"  
Output: true

Input: "Flutter"  
Output: false
